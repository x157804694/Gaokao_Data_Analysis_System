spring:
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://rm-bp1400fo56s5181rq2o.mysql.rds.aliyuncs.com:3306/gaokaotestdb?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: sheepzera
    password: qwe!1234
    type: com.alibaba.druid.pool.DruidDataSource
    # Druid数据源配置
    druid:
      initialSize: 5
      minIdle: 10
      maxActive: 20
      maxWait: 60000
      timeBetweenEvictionRunsMillis: 60000
      minEvictableIdleTimeMillis: 300000
      maxEvictableIdleTimeMillis: 900000
      validationQuery: SELECT 1 FROM DUAL
      testWhileIdle: true
      testOnBorrow: false
      testOnReturn: false
      poolPreparedStatements: true
      filters: stat,wall,log4j
      maxPoolPreparedStatementPerConnectionSize: 20
      useGlobalDataSourceStat: true
      connectionProperties: druid.stat.mergeSql=true;druid.stat.slowSqlMillis=500
# 日志配置
logging:
  level:
    com.hust.gaokao_data_analysis_system.mapper: DEBUG
  file:
    name: log/gaokao.log

# mybatis-plus配置
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: false
